the issue also was, the AI pointed out its in the Ask mode, but switched itself to Agent mode

Next executions might be problematic

update hosts file, to change direction of url
locate a local cert and send it

hide text / make it 1liner, super long ?